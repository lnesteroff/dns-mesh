# Dockerfile for the Knot reconciler script runtime
FROM python:3.9-slim

# Install Python dependencies
RUN pip install kubernetes dnspython

# Set the working directory
WORKDIR /app

# The reconcile.py script will be mounted from a ConfigMap,
# so we don't copy it here. This image is just the runtime environment.
CMD ["python", "./reconcile.py"]
