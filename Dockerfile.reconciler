# Dockerfile for the Knot reconciler script
FROM python:3.9-slim

WORKDIR /app

# Install dependencies
RUN pip install kubernetes dnspython

# Copy the script into the image
COPY reconcile.py .

# Run the script
CMD ["python", "./reconcile.py"]
